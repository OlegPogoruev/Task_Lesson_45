#цель: зависимость
#[tab] команда


BIN = RAW_UDP_Client
SOURCES = main.c
HEADERS = main.h
CC := gcc

all:
	$(CC) $(SOURCES) -o $(BIN).out
$(BIN): $(HEADERS)

run: all
	sudo ./$(BIN).out
#clean:
#	rm -rf $(BIN) *.o $(BIN).out



















